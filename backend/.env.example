# PostgreSQL connection (RDS in production)
DATABASE_URL=postgresql+pg8000://cmm:cmm@localhost:5432/cmm

# CORS origins (frontend URLs)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# App
APP_NAME=CMM
APP_ENV=development
